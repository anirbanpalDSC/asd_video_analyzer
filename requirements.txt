# Python dependencies for the project
# Install the required packages using pip:
# pip install -r requirements.txt

# System-level dependencies:
# - ffmpeg: Required for video processing. Install it using your system's package manager (e.g., apt, brew, choco) or download it from the official website: https://ff
# mpeg.org/download.html
# - whisper: Required for audio transcription. Install it (openai-whisper or compatible)
# - ollama: Required for language model processing. Install it (llama-cpp-python or compatible). Local LLM Server, must be running with a vision model. See https://ollama.com/docs/ for more information.

streamlit>=1.35.0
requests>=2.28.0
Pillow>=9.3.0
openai-whisper>=20240314
pandas>=1.5.0
yt-dlp>=2024.01.01
youtube-transcript-api>=0.6.0
l2cs @ git+https://github.com/Ahmednull/L2CS-Net.git@a4d8f7fa5436a2b2b9f088471623b552a85811bd
mediapipe>=0.10.0
ultralytics>=8.0.0
gdown>=4.7.0
opencv-python>=4.8.0
numpy>=1.24.0
